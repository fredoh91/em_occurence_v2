{% extends 'base.html.twig' %}
{% form_theme form_rech_med 'bootstrap_4_layout.html.twig' %}
{% block head %}
    {{ parent() }}
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
{% endblock head %}
{% block title %}Recherche d'un médicament{% endblock %}

{% block body %}
    
    {{ form_start(form_rech_med) }}
{#    {{ form_widget(form_rech_med) }}#}
{{ form_row(form_rech_med.produit, {'attr':{'placeholder': "Merci de renseigner un nom de produit"}}) }}
    {% if produits == "Pas de produit" %}
{#        Merci de renseigner un nom de produit#}
    {% else %}
        <table class="table table-striped">
            <thead>
              <tr>
                <th>id</th>
                <th>Produit</th>
                <th>Effectif</th>
              </tr>
            </thead>
            <tbody>
        {% for produit in produits %}
              <tr>
                <td>{{ produit.getId }}</td>
                <td>{{ produit.getProduit }}</td>
                <td>{{ produit.getNbr }}</td>
              </tr>
{#            {{ produit.getId }}
            {{ produit.getProduit }}
            {{ produit.getNbr }}#}
        {% endfor %}
            </tbody>
        </table>

    {% endif %}

{#    {{ dump(produits) }}#}
    
    {{ form_end(form_rech_med) }}
    
{#    {{ form(form_rech_med) }}
    {{ dump(produits) }}#}
{#    {{ form(formEmOccProduit) }}#}
    
{#<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    <form method="POST" action=""> 
        <fieldset>
            <table>
                <tr>
                    <td id="t1">
                        <h0 style="color:#2E9AFE"> Médicament</h0> <input type="text" name="prod_search">
                    <td id="t1">
                    <input type="SUBMIT" value="Rechercher"> 
                    </td>
                </tr>
            </table>
        </fieldset>
    </form>
    
    
    This friendly message is coming from:
    <ul>
        <li>Your controller at <code><a href="{{ 'C:/xampp/htdocs/em_occurence_v2/src/Controller/RechercheMedicamentController.php'|file_link(0) }}">src/Controller/RechercheMedicamentController.php</a></code></li>
        <li>Your template at <code><a href="{{ 'C:/xampp/htdocs/em_occurence_v2/templates/recherche_medicament/index.html.twig'|file_link(0) }}">templates/recherche_medicament/index.html.twig</a></code></li>
    </ul>
</div>#}
{% endblock %}
